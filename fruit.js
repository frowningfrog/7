// take in color function

// check the file for that fruit
// if there, print its color
// if not, ask for its color

// then take in color function again
const net = require("net");
const fs = require("fs");

const log_file = "server.log";
const clients = new Map();
